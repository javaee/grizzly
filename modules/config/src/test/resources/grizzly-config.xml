<network-config>
    <transports>
        <transport name="tcp"/>
    </transports>
    <protocols>
        <protocol name="http">
            <http/>
        </protocol>
    </protocols>
    <network-listeners>
        <thread-pool thread-pool-id="defaultThreadPool"/>
        <network-listener name="http-listener" port="8080" transport="tcp" protocol="http" thread-pool="defaultThreadPool"/>
        <network-listener name="https-listener" port="8181" transport="tcp" protocol="http" thread-pool="defaultThreadPool"/>
    </network-listeners>
</network-config>
